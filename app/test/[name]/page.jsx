import React from "react";

export default function TestName({ params }) {
  const { name } = params;
  return (
    <div>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
    </div>
  );
}
